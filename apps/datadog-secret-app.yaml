apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: datadog-secret
  namespace: argocd
  annotations:
    argocd.argoproj.io/sync-wave: "-2"
spec:
  project: default
  source:
    repoURL: "https://github.com/vittomatt/rpi-charts"
    targetRevision: HEAD
    path: secrets/datadog
  destination:
    server: https://kubernetes.default.svc
    namespace: datadog
  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true
