apiVersion: v1
kind: Service
metadata:
  name: prode-postgres-postgresql
  namespace: vittospace
spec:
  type: ClusterIP
  selector:
    app: prode-postgres
  ports:
    - name: postgres
      port: 5432
      targetPort: postgres
